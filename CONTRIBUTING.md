# 开发约定

1. 使用 Node 22，执行 `npm ci`。
2. 在对应模块中修改：宠物放入 `src/features/pet/`，场景放入 `src/scenes/`，纯逻辑保持可测试。
3. 宠物动画不直接改写世界坐标，模型工厂不访问 DOM 或存档。
4. 配置不兼容变更需要版本号和迁移逻辑；不能直接删除旧存档。
5. 纹理、几何体、材质、监听器和帧循环必须有释放逻辑。
6. 提交前执行 `npm run format` 与 `npm run check`；视觉和交互变化需浏览器验证。
7. 不提交密钥、环境文件、个人存档、构建产物或浏览器配置。

GitHub CI 执行与本地相同的检查。提交信息建议使用 `feat:`、`fix:`、`refactor:`、`docs:` 等前缀。

# 浏览器验收记录

## 宠物组件改造（2026-09-07）

- 5 项 Node 测试通过：旧配置迁移与异常值、配置序列化与拒绝非法导入、模型重建资源释放、动作过渡与世界坐标隔离、双实例资源隔离及射线拾取。
- 浏览器验证关闭编辑器丢弃草稿；猫咪/雾灰/条纹/项圈与名字保存正确，刷新后恢复。
- 配置文件导出后重新导入成功；不支持的版本显示明确错误。
- 连续打开关闭 3 次后，页面仅剩 1 个房间画布；组件单测验证旧模型几何体释放。这不等于完整 GPU 内存压力测试。
- 自定义宠物能够完成房间清洁交互，日记写入成功。
- 手机模拟视口 390 × 844，编辑器与页面无横向溢出，滚动后保存可用；桌面预览已检查。
- Chrome 控制台 0 errors / 0 warnings。生产构建通过，保留单 bundle 体积提示。
- 预览截图：output/playwright/pet-component-editor.png、pet-component-mobile.png。

## 初版验证

验证环境：Windows、Chrome、Playwright CLI；2026-09-07。

已通过：

- `npm run build` 生产构建成功。
- 桌面 1440 × 1000：3D 场景正常渲染，已人工查看截图。
- 手机模拟视口 390 × 844：房间完整可见，菜单保留，无页面横向溢出；夜景截图已查看。
- 通过 UI 依次进行摸摸、喂食、玩耍、休息、清洁，五种互动完成后均产生对应日记。
- 三项每日任务全部完成，货币从 128 正确增加至 163。
- 通过选择弹窗将猫切换成柴犬，名字更新为“布丁”。
- 刷新页面后复查宠物、货币、任务和日记存档。
- 白天与夜晚切换，灯光及背景响应正常。
- 修复初检中画布黑底、缺失图标、favicon 404 和手机导航溢出；复查浏览器控制台为 0 errors / 0 warnings。

截图保存在 `output/playwright/desktop.png` 和 `output/playwright/mobile-night.png`（本地验证产物，不加入版本管理）。

边界：手机检查使用浏览器视口模拟，并非真机 GPU/触摸性能测试；程序寻路有障碍物与转角约束，但尚未进行长时间全地图碰撞压力测试。模型与动画为程序生成的风格化实现，不是影视级毛发或动作捕捉资产。构建仍提示 Three.js 所在单个 bundle 超过 500KB，gzip 后约 140KB。

## 半写实宠物更新（2026-09-07）

- 猫狗独立躯干比例、连续尾巴、耳形与口鼻；加入短毛纹理、低密度毛丝和同步闭合的眼睛。
- 四肢两段关节求解、四拍步态、收腿睡姿、低头进食；原外观配置兼容。
- 6 项组件测试通过，新增足端位置与纹理释放检查；最终生产构建通过。
- Chrome 近景检查猫、狗、行走与睡姿；截图位于 output/playwright/realistic-*.png。
- 修复扩大净空后旧食盆站位不可达的问题：调整站位和朝向。四个家具目标从初始位置均可寻路，完整喂食流程已完成并写入日记。
- 当前为程序化半写实改进，不是照片级毛发或完整骨骼蒙皮模型；未进行新增毛丝效果的手机真机性能测试。

## Pawstice 工程化整理（2026-09-07）

- 入口、游戏编排、界面、房间场景、寻路、存档、宠物分别独立；历史文档移入 docs。
- ESLint、Prettier、8 项单元测试及生产构建通过。
- 开发环境检查宠物编辑器和单画布清理；生产预览验证改名保存、摸摸完成及日记。生产不暴露调试对象。
- 旧存档键保持不变；新增结构与无效值兼容测试。
- GitHub CI 定义完整质量检查，不启用自动部署。
